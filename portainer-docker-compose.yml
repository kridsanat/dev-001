
version: "3"

services:
  portainer:
    image: portainer/portainer-ce:latest
    ports:
      - 8890:9000

    volumes:
        - data:/data
        - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped\
volumes:
  data:

networks:
  default:
    name: m_network  